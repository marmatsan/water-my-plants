# Standalone Verification Platform Consumer

This fixture resolves and applies the published verification plugin from the
staged Maven repository. It contains no source `includeBuild` and executes only
the public application contract, not repository-specific verification tasks.
