# Standalone Unit Test DSL Consumer

This fixture is executed only by `repo/unit-testing:verifyStagedPublication`.
It resolves the DSL from a staged Maven repository and deliberately contains
no `includeBuild` declaration.
